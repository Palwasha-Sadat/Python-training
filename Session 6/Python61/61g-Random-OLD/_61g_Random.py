import random  # Import the random module to use randint for generating random numbers

for i in range(10):
    # Print a random number between 1 and 100 (you can adjust the range as needed)
    random_number = random.randint(1, 100)
    print(random_number)
